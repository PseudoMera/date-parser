export type PseudoDate = {
    day: string
    month: string
    year: string
    dayOfTheWeek: string
    hour: string
    minutes: string
    seconds: string
    miliseconds: string
    amOrPm: string
    monthNumber: string
    monthShort: string
  }